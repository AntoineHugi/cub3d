#include "../inc/cub3d.h"
/*
void	render_map(t_game *game)
{
	Cast rays per vertical column of pixels to find:
   		The distance to the nearest wall in that direction.
		The exact position on the wall texture to sample for that vertical slice.
	Calculate the height of the wall slice on screen, based on the distance.
	Draw that vertical slice of wall at the correct height in the column.
	Fill everything above the wall slice with the ceiling color.
	Fill everything below the wall slice with the floor color.
}
*/